#include <iostream>
#include "matrix.h"

int main() {
    std::cout << "Transpose Funtion!" << std::endl;
    int rows = 2;
    int cols = 3;
    for (int i = 0; i < rows; i++)
        std::cout << i << std::endl;
        for (int j = 0; j < cols; j++){
            std::cout << j << std::endl;
        }
}